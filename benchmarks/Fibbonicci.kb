using std::io

fn Fibbonicci(n : int) {
    if n <= 1 {
        return n
    }

    return Fibbonicci(n - 1) + Fibbonicci(n - 2)
}

fn main() {
    compiletime {
        Start = 35
    }

    Result = Fibbonicci(Start)
}